import { describe,expect,it } from "vitest";import { buildMonthlyReport } from "./reports";import { MemoryDbAdapter } from "@/lib/data/memory";
describe("reports",()=>{it("excludes refunded and unsettled",async()=>{const db=new MemoryDbAdapter();const purchase=await db.createPurchase({productName:"鞋",styleCode:"STYLE-REPORT",platform:"taobao",unitPriceCents:10000,quantity:2,purchasedAt:"2026-01-01",size:"42",initialStatus:"arrived",orderNo:"",note:""});await db.settleUnits({unitIds:[purchase.unitIds[0]],actualPayoutCents:15000,settledAt:"2026-01-02"});await db.refundUnit({unitId:purchase.unitIds[0]});const state=db.snapshot();const report=buildMonthlyReport({units:state.units,products:state.products,batches:state.batches,sales:state.sales,month:"2026-01",basis:"settled"});expect(report.actualProfitTotal).toBe(0);expect(report.activeStockCost).toBe(10000);});});
