import type { DbAdapter, CleanupResult, ClearResult, DeleteResult, PurchaseInput, PurchaseResult, SaveAttachmentInput, ShipUnitsInput, ShipUnitsResult, StatusChangeInput } from "@/lib/data/types";
import type { Attachment, InventoryUnit, Product, PurchaseBatch, Sale, StatusHistory, StorageDeletionJob } from "@/lib/types/database";
import { allocateShippingCents } from "@/lib/services/shipping";
import { assertCents } from "@/lib/utils/money";

export interface MemoryState { products: Product[]; batches: PurchaseBatch[]; units: InventoryUnit[]; sales: Sale[]; history: StatusHistory[]; attachments: Attachment[]; cleanupJobs: StorageDeletionJob[]; }
const clone = <T,>(value: T): T => structuredClone(value);
const now = (): string => new Date().toISOString();
const id = (): string => crypto.randomUUID();

export class MemoryDbAdapter implements DbAdapter {
  readonly kind = "memory" as const;
  private state: MemoryState;
  private failureAfterMutation: number | null = null;
  constructor(seed: Partial<MemoryState> = {}) {
    this.state = clone({
      products: seed.products ?? [],
      batches: seed.batches ?? [],
      units: seed.units ?? [],
      sales: seed.sales ?? [],
      history: seed.history ?? [],
      attachments: seed.attachments ?? [],
      cleanupJobs: seed.cleanupJobs ?? [],
    });
  }
  snapshot(): MemoryState { return clone(this.state); }
  injectFailureAfter(mutationCount: number | null): void { this.failureAfterMutation = mutationCount; }
  private transaction<T>(work: (draft: MemoryState, mutate: () => void) => T): T { const draft = clone(this.state); let count = 0; const mutate = () => { count += 1; if (this.failureAfterMutation != null && count >= this.failureAfterMutation) throw new Error("注入事务故障"); }; const result = work(draft, mutate); this.state = draft; return result; }
  async listProducts() { return clone(this.state.products); } async listBatches() { return clone(this.state.batches); } async listUnits() { return clone(this.state.units); } async listSales() { return clone(this.state.sales); }
  async listHistory(unitId?: string) { return clone(unitId ? this.state.history.filter((row) => row.unit_id === unitId) : this.state.history); }
  async listAttachments(ownerType: Attachment["owner_type"], ownerId?: string) { return clone(this.state.attachments.filter((row) => row.owner_type === ownerType && (!ownerId || row.owner_id === ownerId))); }
  async attachmentUrl(attachment: Attachment) { return `memory://${attachment.path}`; }
  async saveAttachment(input: SaveAttachmentInput) { const row: Attachment = { id: id(), user_id: "test-user", owner_type: input.owner_type, owner_id: input.owner_id, kind: input.kind, path: `test-user/${input.owner_type}/${input.owner_id}/${id()}`, content_type: input.file.type || null, created_at: now() }; this.state.attachments.push(row); return clone(row); }
  async createPurchase(input: PurchaseInput): Promise<PurchaseResult> { return this.transaction((draft, mutate) => { assertCents(input.unitPriceCents); if (!input.productName.trim()) throw new Error("请填写品名"); const styleCode = input.styleCode.trim(); if (!styleCode) throw new Error("请填写货号"); if (!Number.isSafeInteger(input.quantity) || input.quantity < 1 || input.quantity > 999) throw new Error("数量需为 1 至 999 的整数"); const timestamp = now(); let product = draft.products.find((row) => row.style_code?.toLowerCase() === styleCode.toLowerCase()); if (!product) { product = { id: id(), user_id: "test-user", name: input.productName, style_code: styleCode, brand: null, created_at: timestamp, updated_at: timestamp }; draft.products.push(product); mutate(); } const batch: PurchaseBatch = { id: id(), user_id: "test-user", product_id: product.id, platform: input.platform, order_no: input.orderNo || null, unit_price_cents: input.unitPriceCents, quantity: input.quantity, shipping_fee_cents: 0, discount_amount_cents: 0, purchased_at: input.purchasedAt, note: input.note || null, created_at: timestamp, updated_at: timestamp }; draft.batches.push(batch); mutate(); const unitIds: string[] = []; for (let index = 0; index < input.quantity; index += 1) { const unit: InventoryUnit = { id: id(), user_id: "test-user", batch_id: batch.id, product_id: product.id, size: input.size, unit_cost_cents: input.unitPriceCents, listing_price_cents: null, outbound_shipping_cents: 0, status: input.initialStatus, created_at: timestamp, updated_at: timestamp }; draft.units.push(unit); draft.history.push({ id: id(), user_id: "test-user", unit_id: unit.id, from_status: null, to_status: input.initialStatus, note: "采购入库", created_at: timestamp }); unitIds.push(unit.id); mutate(); } return { productId: product.id, batchId: batch.id, unitIds }; }); }
  async shipUnits(input: ShipUnitsInput): Promise<ShipUnitsResult> { return this.transaction((draft, mutate) => { const unique = new Set(input.unitIds); if (!input.unitIds.length || unique.size !== input.unitIds.length) throw new Error("商品 ID 无效或重复"); const units = input.unitIds.map((unitId) => draft.units.find((row) => row.id === unitId)); if (units.some((unit) => !unit || unit.status === "refunded")) throw new Error("所选商品不存在或已采购退款"); const valid = units as InventoryUnit[]; const overwrittenUnitIds = valid.filter((unit) => unit.outbound_shipping_cents > 0).map((unit) => unit.id); if (overwrittenUnitIds.length && !input.overwriteConfirmed) throw new Error("存在旧分摊值，请确认覆盖"); const allocations = allocateShippingCents(valid.map((unit) => ({ id: unit.id, createdAt: unit.created_at, currentShippingCents: unit.outbound_shipping_cents })), input.totalShippingCents); for (const allocation of allocations) { const unit = valid.find((row) => row.id === allocation.unitId)!; const from = unit.status; unit.outbound_shipping_cents = allocation.shippingCents; unit.status = "shipping"; unit.updated_at = now(); draft.history.push({ id: id(), user_id: unit.user_id, unit_id: unit.id, from_status: from, to_status: "shipping", note: overwrittenUnitIds.includes(unit.id) ? "覆盖寄出运费" : "批量寄出", created_at: now() }); mutate(); } return { allocations, totalShippingCents: input.totalShippingCents, overwrittenUnitIds }; }); }
  async settleUnits(input: { unitIds: string[]; actualPayoutCents: number; settledAt: string }) { this.transaction((draft, mutate) => { assertCents(input.actualPayoutCents); if (!input.unitIds.length || new Set(input.unitIds).size !== input.unitIds.length) throw new Error("商品 ID 无效或重复"); for (const unitId of input.unitIds) { const unit = draft.units.find((row) => row.id === unitId); if (!unit || unit.status === "refunded") throw new Error("商品不存在或已退款"); const existing = draft.sales.find((row) => row.unit_id === unitId); const timestamp = now(); const sale: Sale = { id: existing?.id ?? id(), user_id: unit.user_id, unit_id: unit.id, sold_price_cents: 0, platform_fee_cents: 0, platform_subsidy_cents: 0, express_fee_cents: 0, other_fee_cents: 0, actual_payout_cents: input.actualPayoutCents, sold_at: input.settledAt, settled_at: input.settledAt, created_at: existing?.created_at ?? timestamp, updated_at: timestamp }; if (existing) Object.assign(existing, sale); else draft.sales.push(sale); const from = unit.status; unit.status = "settled"; draft.history.push({ id: id(), user_id: unit.user_id, unit_id: unit.id, from_status: from, to_status: "settled", note: "登记到手价", created_at: timestamp }); mutate(); } }); }
  async changeStatus(input: StatusChangeInput) { this.transaction((draft, mutate) => { if (!input.unitIds.length || new Set(input.unitIds).size !== input.unitIds.length) throw new Error("商品 ID 无效或重复"); for (const unitId of input.unitIds) { const unit = draft.units.find((row) => row.id === unitId); if (!unit) throw new Error("库存不存在"); const from = unit.status; unit.status = input.toStatus; if (!(["sold", "settled"] as string[]).includes(input.toStatus)) draft.sales = draft.sales.filter((row) => row.unit_id !== unitId); draft.history.push({ id: id(), user_id: unit.user_id, unit_id: unit.id, from_status: from, to_status: input.toStatus, note: input.note?.trim() || null, created_at: now() }); mutate(); } }); }
  async refundUnit(input: { unitId: string; note?: string }) { this.transaction((draft, mutate) => { const unit = draft.units.find((row) => row.id === input.unitId); if (!unit) throw new Error("库存不存在"); const from = unit.status; draft.sales = draft.sales.filter((row) => row.unit_id !== unit.id); unit.status = "refunded"; draft.history.push({ id: id(), user_id: unit.user_id, unit_id: unit.id, from_status: from, to_status: "refunded", note: input.note?.trim() || "采购平台退货退款", created_at: now() }); mutate(); }); }
  async deleteUnitDeep(input: { unitId: string }): Promise<DeleteResult> { return this.transaction((draft, mutate) => { const unit = draft.units.find((row) => row.id === input.unitId); if (!unit) throw new Error("库存不存在"); const saleIds = draft.sales.filter((row) => row.unit_id === unit.id).map((row) => row.id); const owners = new Set([unit.id, ...saleIds]); const paths: string[] = []; draft.attachments = draft.attachments.filter((attachment) => { const remove = (attachment.owner_type === "unit" && owners.has(attachment.owner_id)) || (attachment.owner_type === "sale" && owners.has(attachment.owner_id)); if (remove) paths.push(attachment.path); return !remove; }); draft.sales = draft.sales.filter((row) => row.unit_id !== unit.id); draft.history = draft.history.filter((row) => row.unit_id !== unit.id); draft.units = draft.units.filter((row) => row.id !== unit.id); let deletedBatch = false; let deletedProduct = false; if (!draft.units.some((row) => row.batch_id === unit.batch_id)) { const batchPaths = draft.attachments.filter((row) => row.owner_type === "batch" && row.owner_id === unit.batch_id).map((row) => row.path); paths.push(...batchPaths); draft.attachments = draft.attachments.filter((row) => !(row.owner_type === "batch" && row.owner_id === unit.batch_id)); draft.batches = draft.batches.filter((row) => row.id !== unit.batch_id); deletedBatch = true; } if (!draft.units.some((row) => row.product_id === unit.product_id) && !draft.batches.some((row) => row.product_id === unit.product_id)) { const productPaths = draft.attachments.filter((row) => row.owner_type === "product" && row.owner_id === unit.product_id).map((row) => row.path); paths.push(...productPaths); draft.attachments = draft.attachments.filter((row) => !(row.owner_type === "product" && row.owner_id === unit.product_id)); draft.products = draft.products.filter((row) => row.id !== unit.product_id); deletedProduct = true; } for (const path of paths) draft.cleanupJobs.push({ id: id(), user_id: unit.user_id, path, created_at: now(), completed_at: null }); mutate(); return { deletedUnitId: unit.id, deletedBatch, deletedProduct, pendingStoragePaths: paths }; }); }
  async clearAllData(input: { confirmation: "清空" }): Promise<ClearResult> { if (input.confirmation !== "清空") throw new Error("确认词错误"); return this.transaction((draft, mutate) => { const result: ClearResult = { products: draft.products.length, batches: draft.batches.length, units: draft.units.length, sales: draft.sales.length, history: draft.history.length, attachments: draft.attachments.length, pendingStoragePaths: draft.attachments.map((row) => row.path) }; for (const path of result.pendingStoragePaths) draft.cleanupJobs.push({ id: id(), user_id: "test-user", path, created_at: now(), completed_at: null }); draft.products = []; draft.batches = []; draft.units = []; draft.sales = []; draft.history = []; draft.attachments = []; mutate(); return result; }); }
  async retryStorageCleanup(): Promise<CleanupResult> { const pending = this.state.cleanupJobs.filter((row) => row.completed_at == null); pending.forEach((row) => { row.completed_at = now(); }); return { attempted: pending.length, completed: pending.length, pending: 0, failedPaths: [] }; }
}
